import { CameraConfig } from "../../../CameraConfigTypes";

export const cameraConfig: CameraConfig = {
  position: [0, 0, 5],
  target: [0, 0, 0],
  duration: 4.2,
  ease: "circ.inOut",
  fov: 75,
  type: "static",
  useBmwRig: true,
  shakeIntensity: 0,
};
