import { CameraConfig } from "../../../CameraConfigTypes";

export const cameraConfig: CameraConfig = {
  position: [0, 2, 6],
  target: [0, 0.5, 0],
  duration: 5,
  ease: "power2.inOut",
  fov: 50,
  type: "static",
};
